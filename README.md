# frontend-staj-gorev2
HTML + CSS ile kişisel sayfa tasarımı

GÖREV 2 — HTML + CSS (Temel Stil Tasarımı)
🎯 Konu:

HTML yapısına CSS ekleyerek sayfayı görsel olarak düzenlemek.
Artık yalnızca içerik değil, tasarım (renk, hizalama, yazı tipi, arka plan) da devreye giriyor.

🧱 İçerik:

Bu görevde proje şu dosyalardan oluşacak:

frontend-staj-gorev2/
│
├── index.html
└── style.css


index.html → yapıyı oluşturur
style.css → görünümü düzenler (renkler, font, hizalama, arka plan vs.)

💡 Amaç:

CSS’i HTML’e bağlamak (<link rel="stylesheet" href="style.css">)

Temel CSS özelliklerini öğrenmek:

color, background-color

font-family, text-align

margin, padding, border

width, height

Sayfanın görünümünü geliştirmek, örneğin:

Başlık ortada

Arka plan rengi açık

İletişim kısmı kutu içinde
